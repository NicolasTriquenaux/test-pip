import os
from setuptools import setup


setup(name='test-pip',
      version='0.1',
      description='(private) somedesc',
      url='',
      author='dataiku',
      author_email='qa@dataiku.com',
      license='',
      packages=["test-pip"],
      zip_safe=False)
