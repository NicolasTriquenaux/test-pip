import os
from setuptools import setup


setup(name='test-pip',
      version='0.1',
      description='(private) somedesc',
      url='',
      author='trololo',
      author_email='troll@trololo.com',
      license='',
      packages=["test-pip"],
      zip_safe=False)
